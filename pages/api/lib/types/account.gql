

type AccountType {
    _id: ID
    idUser: ID
}
# Account Type

# Account Input
input IAccount {
    _id: ID
    idUser: ID
}
enum typeAccount {
  ASSETS,
  EXPENSE,
  LIABILITY,
  INCOME,
}
# Query
type Query {
  getAllAccount(idComp: ID): [AccountType]
}

# Mutations
type Mutation {
  registerAccount(input: IAccount): AccountType
}
